---
phase: 07-shared-pipeline-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/tests/capture_golden.py
  - pipeline/tests/golden/link_resolver_all.diff
  - pipeline/tests/golden/frontmatter_111.json
  - pipeline/tests/golden/next_batch.json
  - pipeline/tests/golden/status.txt
autonomous: true
requirements: [SC-2a, SC-2c, SC-3, SC-3b]
must_haves:
  truths:
    - "The gitignored, git-unrecoverable import state is backed up outside the repo before any production file is touched"
    - "Four BEFORE fingerprints of current pipeline behaviour exist as committed, tracked fixtures"
    - "Capturing the fingerprints leaves laws/israel/ byte-identical to HEAD"
  artifacts:
    - path: "pipeline/tests/capture_golden.py"
      provides: "One-shot BEFORE-fingerprint capture for all four gates"
      contains: "def main"
    - path: "pipeline/tests/golden/frontmatter_111.json"
      provides: "111-entry build_frontmatter mapping, frozen clock"
    - path: "pipeline/tests/golden/next_batch.json"
      provides: "get_next_batch selection at counts 1/5/25/100"
    - path: "pipeline/tests/golden/status.txt"
      provides: "Exact 6-line --status stdout"
    - path: "pipeline/tests/golden/link_resolver_all.diff"
      provides: "Diff the pipeline produces over laws/israel/ from a clean HEAD"
  key_links:
    - from: "pipeline/tests/capture_golden.py"
      to: "pipeline/reconcile.py"
      via: "sys.path.insert(PIPELINE_DIR) then bare import"
      pattern: "sys\\.path\\.insert"
    - from: "pipeline/tests/capture_golden.py"
      to: "pipeline/tests/golden/"
      via: "write_text of each fingerprint"
      pattern: "golden"
---

<objective>
Build the safety net for Phase 7 before a single line of production code moves: back up the
gitignored import state off-repo, then capture the four BEFORE fingerprints that every later
extraction step will be gated against.

Purpose: CLAUDE.md — "Compare outputs before updating pipeline logic" and "Keep fixtures and
golden outputs". Nothing in this phase is provable without these artifacts existing first.
Output: `pipeline/tests/capture_golden.py`, four committed fixtures under `pipeline/tests/golden/`,
and a verified off-repo copy of `import_progress.json` and `manifest_laws.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shared-pipeline-core/07-RESEARCH.md
@.planning/phases/07-shared-pipeline-core/07-PATTERNS.md
@.planning/phases/07-shared-pipeline-core/07-VALIDATION.md
@CLAUDE.md

<interfaces>
Contracts this plan consumes. All line numbers re-verified in the working tree, 2026-08-09.

pipeline/reconcile.py
  build_frontmatter(entry: dict) -> str        line 120; calls dt.datetime.now(dt.timezone.utc) at line 125
  build_seo_description(title, pub_date, law_validity) -> str
  MODEL                                        module constant embedded into the frontmatter

pipeline/batch_import.py
  load_manifest() -> list[dict]                line 63
  load_progress() -> dict                      line 47
  get_next_batch(manifest, progress, count) -> list[dict]    line 255
  print_status(manifest, progress) -> None     line 296
  PROGRESS_PATH = DATA_DIR / "import_progress.json"          line 37
  SITE_DIR      = PROJECT_DIR / "site"                       line 35

pipeline/link_resolver.py
  main(law_ids=None, relink_all=False) -> int  line 235; CLI block at 258-265; --all mutates laws/israel/ in place

Data on disk, both GITIGNORED (.gitignore line 2 = data/raw/) so git cannot restore them:
  data/raw/israel/import_progress.json   keys done/failed/total_deployed/priority; 1867 bytes; ends with '}' and NO trailing newline
  data/raw/israel/manifest_laws.json     1076 entries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Back up the git-unrecoverable import state off-repo</name>
  <files>(no repo files - writes only to $HOME/codex-civica-backups/phase07/)</files>
  <read_first>
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, sections "Runtime State Inventory" and "Pitfall 3"
    - .gitignore (confirm line 2 is data/raw/)
  </read_first>
  <action>
    Create `$HOME/codex-civica-backups/phase07/` — outside the repository tree, it must NOT be under
    /mnt/c/Dev/codex-civica. Copy `data/raw/israel/import_progress.json` and
    `data/raw/israel/manifest_laws.json` into it with `cp -p`.

    Prove the copies are exact with `cmp` on both pairs, and record `sha256sum` of all four files
    into `$HOME/codex-civica-backups/phase07/MANIFEST.sha256`.

    Record in the plan SUMMARY the exact restore command a future session would use:
    `cp $HOME/codex-civica-backups/phase07/import_progress.json data/raw/israel/`.

    This task writes nothing inside the repo and stages nothing.

    Rationale: `data/raw/` is gitignored, so `git checkout` cannot restore these files.
    `import_progress.json` holds the only record of which 111 laws are converted, the 23-entry
    priority queue, and `total_deployed`. `done` could be rebuilt from `ls laws/israel/`; `priority`
    and `total_deployed` could not.
  </action>
  <verify>
    <automated>mkdir -p "$HOME/codex-civica-backups/phase07" && cmp data/raw/israel/import_progress.json "$HOME/codex-civica-backups/phase07/import_progress.json" && cmp data/raw/israel/manifest_laws.json "$HOME/codex-civica-backups/phase07/manifest_laws.json" && test -s "$HOME/codex-civica-backups/phase07/MANIFEST.sha256" && echo BACKUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cmp` between the live and backed-up `import_progress.json` exits 0
    - `cmp` between the live and backed-up `manifest_laws.json` exits 0
    - `MANIFEST.sha256` exists and contains 4 sha256 lines (`wc -l` returns 4)
    - Backup path is outside the repo: `realpath "$HOME/codex-civica-backups/phase07"` does not start with `/mnt/c/Dev/codex-civica`
    - `git status --porcelain` shows no new untracked path under `data/`
  </acceptance_criteria>
  <done>Both gitignored state files have a verified byte-identical copy outside the repository, with recorded checksums and a documented restore command.</done>
</task>

<task type="auto">
  <name>Task 2: Write pipeline/tests/capture_golden.py</name>
  <files>pipeline/tests/capture_golden.py</files>
  <read_first>
    - .planning/phases/07-shared-pipeline-core/07-PATTERNS.md, section "pipeline/tests/capture_golden.py" (bootstrap pattern, frozen-clock shim, fingerprint pattern) and section "Shared Patterns" (module header, UTF-8 I/O, sys.path bootstrap, interpreter)
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, section "Code Examples" — all four capture snippets, each already prototyped and executed with a recorded BEFORE value
    - pipeline/backfill_seo_meta.py — the analog: stdlib-only standalone script with sys.path.insert, bare pipeline import, main(), and an argv flag block
    - pipeline/reconcile.py lines 120-184 (build_frontmatter)
    - pipeline/batch_import.py lines 43-70 and 255-313
  </read_first>
  <action>
    Create `pipeline/tests/capture_golden.py`. Stdlib only. No pytest, no PyYAML, no new dependency.

    Header per 07-PATTERNS.md "Module header / import ordering": shebang, module docstring,
    `from __future__ import annotations`, alphabetised stdlib imports, then the bootstrap. This
    script lives one level below `pipeline/`, so the bootstrap is
    `PIPELINE_DIR = Path(__file__).parent.parent`, `PROJECT_DIR = PIPELINE_DIR.parent`,
    `sys.path.insert(0, str(PIPELINE_DIR))`, with post-bootstrap imports carrying `# noqa: E402`
    (existing convention, `backfill_seo_meta.py` lines 12-13). Define
    `GOLDEN_DIR = Path(__file__).parent / "golden"` and mkdir it with `parents=True, exist_ok=True`.

    Implement four capture functions, each writing one fixture into GOLDEN_DIR:

    1. `capture_frontmatter()` writing `golden/frontmatter_111.json`.
       Import `reconcile`. Install the frozen-clock shim from 07-PATTERNS.md — rebind `reconcile.dt`
       to a shim class exposing `timezone` and a nested `datetime` with a static
       `now(tz=None)` returning 1 Jan 2000 UTC — BEFORE calling `build_frontmatter`. Without it,
       `reconcile.py:125` makes every run differ. Do NOT modify `reconcile.py` to accept an
       injectable clock; the hack lives in the harness only.
       Load the manifest and progress JSON, build a mapping
       `{str(law_id or bill_id): build_frontmatter(entry)}` restricted to entries whose id is in
       `progress["done"]`, and write `json.dumps(mapping, ensure_ascii=False, sort_keys=True)` with
       `encoding="utf-8"`. Print `entries=<n> sha256=<hex>` where the hash is sha256 of that exact
       serialised blob.

    2. `capture_batch()` writing `golden/next_batch.json`.
       Import `batch_import`, call `load_manifest()` and `load_progress()` — read-only, never
       `save_progress`. For each count in (1, 5, 25, 100) record the ordered list of selected ids.
       Write `json.dumps(mapping, ensure_ascii=False, sort_keys=True)`.

    3. `capture_status()` writing `golden/status.txt`.
       Shell out with list-form `subprocess.run([sys.executable, str(PIPELINE_DIR / "batch_import.py"),
       "--status"], capture_output=True, text=True, cwd=str(PROJECT_DIR))` and write `result.stdout`
       verbatim. Do NOT re-implement `print_status` inside the harness — the gate is on the real
       CLI's stdout. `--status` is the ONLY permitted invocation form of `batch_import.py` in this
       phase (standing user instruction: factory import is paused at 111/718).

    4. `capture_link_resolver()` writing `golden/link_resolver_all.diff`.
       Exact sequence: `git checkout -- laws/israel/`; purge bytecode with
       `find pipeline -name __pycache__ -type d -exec rm -rf {} +`; run
       `[sys.executable, str(PIPELINE_DIR / "link_resolver.py"), "--all"]` with cwd=PROJECT_DIR,
       discarding stdout; capture `git diff laws/israel/` into the fixture;
       `git checkout -- laws/israel/`; then assert `git status --porcelain laws/israel/` is empty and
       return non-zero if it is not. All git calls use list-form `subprocess.run`, never
       `shell=True`, and the script never calls `git add`.

    `main()` returns `int` (0 = all captured, 1 = any failure) and the CLI block is
    `raise SystemExit(main())`, mirroring `link_resolver.py` lines 258-265. Support `--only NAME` to
    re-capture a single fixture. Configure logging inside `main()` with
    `logging.basicConfig(level=logging.INFO, format="%(levelname)s %(message)s")`, not at import.
    Use `%s`-style lazy logging args, never f-strings inside logging calls.

    Do not run the script in this task — running it is Task 3.
  </action>
  <verify>
    <automated>~/.venv-codex/bin/python -c "import ast; ast.parse(open('pipeline/tests/capture_golden.py').read()); print('PARSE_OK')" && grep -q 'sys.path.insert' pipeline/tests/capture_golden.py && grep -q 'raise SystemExit(main())' pipeline/tests/capture_golden.py && test "$(grep -v '^#' pipeline/tests/capture_golden.py | grep -c 'shell=True')" = 0 && echo SHAPE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/tests/capture_golden.py` parses under `~/.venv-codex/bin/python`
    - Defines four capture functions plus `main() -> int`, and ends with `raise SystemExit(main())`
    - Source assertion: contains `PIPELINE_DIR = Path(__file__).parent.parent` and `sys.path.insert(0, str(PIPELINE_DIR))`
    - Source assertion: `grep -v '^#' pipeline/tests/capture_golden.py | grep -c 'shell=True'` returns 0
    - Source assertion: every line matching `batch_import.py` also contains `--status`
    - Source assertion: `grep -v '^#' pipeline/tests/capture_golden.py | grep -c '"add"'` returns 0 (no git add)
    - Source assertion: contains `reconcile.dt = ` (frozen-clock shim installed)
    - Source assertion: `grep -v '^#' pipeline/tests/capture_golden.py | grep -c 'import yaml'` returns 0
    - `pipeline/tests/golden/` is created by the script at runtime, not hand-committed empty
  </acceptance_criteria>
  <done>A stdlib-only capture harness exists that can produce all four BEFORE fingerprints and always restores laws/israel/ after the link-resolver run.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the four BEFORE fingerprints and commit them as fixtures</name>
  <files>pipeline/tests/golden/frontmatter_111.json, pipeline/tests/golden/next_batch.json, pipeline/tests/golden/status.txt, pipeline/tests/golden/link_resolver_all.diff</files>
  <read_first>
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, sections "Code Examples" (recorded BEFORE values), "Pitfall 1" (redefined gate), "Pitfall 2" (restore discipline), "Pitfall 5" (interpreter), "Pitfall 6" (auto-deploy on push)
    - pipeline/tests/capture_golden.py (written in Task 2)
    - .planning/phases/07-shared-pipeline-core/07-VALIDATION.md, section "Wave 0 Requirements"
  </read_first>
  <action>
    Confirm the tree is clean apart from the new harness file, then run
    `~/.venv-codex/bin/python pipeline/tests/capture_golden.py` from the repo root. Always use that
    absolute interpreter path — ambient `python3` resolves to the in-repo NTFS `.venv/`, which lacks
    `google-genai` and `python-dotenv`, and the resulting ModuleNotFoundError reads like a refactor
    regression.

    Compare the printed values against the BEFORE values research already recorded. These are hard
    expectations, not advisory:
      - frontmatter: entries=111 and sha256 = dab1887e06dd074051ed6a2eff2c2fcdabff1a2f523c8a3cc93fd20c7168dd37
      - status.txt: exactly 6 lines — "Total laws:      1076", "With PDF:        718",
        "Converted:       111", "Failed:          0", "Pending:         607", "Total deployed:  111"
      - next_batch: the first three ids at every count of 5 or more are 2001111, 2001108, 2001070
      - link_resolver_all.diff: NON-empty, expected around 8 files / 22 insertions / 22 deletions

    The non-empty link-resolver diff is expected and correct. It is pre-existing Class-A staleness
    plus the deferred Class-B `_STRIP_MG_INDEX` bug (research Pitfall 1). It is NOT a defect
    introduced here. Do not fix it — the user deferred that fix to a v1.0 follow-up, and fixing it
    would change laws/israel/ content and destroy the only property that makes this refactor
    verifiable. Do not touch `_STRIP_MG_INDEX`.

    If the frontmatter sha256 does NOT equal dab1887e…, STOP and report. Something changed in
    reconcile.py, the manifest, or the progress file since research, and every downstream gate value
    would need re-baselining. Never silently accept a different hash — CLAUDE.md: "Validation errors
    must never be silently ignored."

    Then assert `git status --porcelain laws/israel/` is empty. Stage explicitly with
    `git add pipeline/tests/` and nothing else. NEVER use `git add -A` in this phase: a proof run
    leaves mutated tracked law files, and `.github/workflows/deploy.yml` publishes to production
    GitHub Pages on every push to main with no path filter.
  </action>
  <verify>
    <automated>test -s pipeline/tests/golden/frontmatter_111.json && test -s pipeline/tests/golden/next_batch.json && test -s pipeline/tests/golden/status.txt && test -s pipeline/tests/golden/link_resolver_all.diff && ~/.venv-codex/bin/python -c "import hashlib,pathlib; b=pathlib.Path('pipeline/tests/golden/frontmatter_111.json').read_text(encoding='utf-8'); h=hashlib.sha256(b.encode()).hexdigest(); assert h=='dab1887e06dd074051ed6a2eff2c2fcdabff1a2f523c8a3cc93fd20c7168dd37', h; print('SHA_OK')" && test -z "$(git status --porcelain laws/israel/)" && echo TREE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - All four fixture files exist and are non-empty
    - sha256 of `golden/frontmatter_111.json` equals `dab1887e06dd074051ed6a2eff2c2fcdabff1a2f523c8a3cc93fd20c7168dd37`
    - `~/.venv-codex/bin/python -c "import json;print(len(json.load(open('pipeline/tests/golden/frontmatter_111.json'))))"` prints 111
    - `wc -l < pipeline/tests/golden/status.txt` returns 6
    - `grep -c 'Converted:       111' pipeline/tests/golden/status.txt` returns 1 and `grep -c 'Failed:          0' pipeline/tests/golden/status.txt` returns 1
    - `~/.venv-codex/bin/python -c "import json;d=json.load(open('pipeline/tests/golden/next_batch.json'));assert d['5'][:3]==['2001111','2001108','2001070'],d['5'][:3];print('BATCH_OK')"`
    - `test -z "$(git status --porcelain laws/israel/)"` passes
    - `git diff --cached --name-only` lists only paths under `pipeline/tests/`
  </acceptance_criteria>
  <done>Four committed, tracked BEFORE fixtures exist; the frontmatter hash matches the research baseline exactly; laws/israel/ is byte-identical to HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem to pipeline scripts | Local JSON/Markdown (trusted but unvalidated) crosses here via `json.load` and `read_text` |
| pipeline to git working tree (`laws/israel/`) | In-place mutation of tracked content that auto-publishes on push to `main` |
| pipeline to external process | `subprocess.run` boundary carrying the inherited environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | `pipeline/tests/capture_golden.py` importing `reconcile` (which runs `load_dotenv` on `pipeline/.env`) | mitigate | Harness never reads, prints, or writes `GEMINI_API_KEY`; fixtures contain only frontmatter/manifest data. Assert `grep -c 'GEMINI_API_KEY' pipeline/tests/` returns 0 (ASVS V14) |
| T-07-02 | Tampering (integrity) | `laws/israel/*.md` plus auto-deploy on push to `main` | mitigate | Every proof run ends with `git checkout -- laws/israel/`; `test -z "$(git status --porcelain laws/israel/)"` before commit; explicit `git add pipeline/tests/`, never `git add -A` (ASVS V12) |
| T-07-03 | Tampering | `subprocess.run` calls to git / python in the harness | mitigate | List-form argv only, no `shell=True`, no caller-supplied string interpolated into argv (ASVS V7) |
| T-07-04 | Denial of service | `data/raw/israel/import_progress.json` (gitignored, unrecoverable) | mitigate | Task 1 off-repo backup with `cmp` verification; harness is read-only on progress and never calls `save_progress` |
| T-07-SC | Tampering | pip installs | accept | Zero new runtime dependencies in this phase (research Package Legitimacy Audit: N/A). No install task exists, so no legitimacy checkpoint applies. `pytest` is deliberately NOT installed |
</threat_model>

<verification>
- `cmp` passes on both off-repo backup pairs
- All four golden fixtures exist, non-empty, and committed under `pipeline/tests/golden/`
- Frontmatter sha256 equals the research baseline `dab1887e…`
- `git status --porcelain laws/israel/` empty
- No file outside `pipeline/tests/` is staged or modified
</verification>

<success_criteria>
A future session can, from these artifacts alone, detect any behaviour change in
`build_frontmatter`, `get_next_batch`, `print_status`, or `link_resolver --all` — and can restore
the gitignored import state if it is ever corrupted.
</success_criteria>

<output>
Create `.planning/phases/07-shared-pipeline-core/07-01-SUMMARY.md` when done. Record the off-repo
backup path, the restore command, and the four captured fingerprint values verbatim.
</output>
