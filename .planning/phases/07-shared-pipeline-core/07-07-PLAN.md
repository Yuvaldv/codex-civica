---
phase: 07-shared-pipeline-core
plan: 07
type: execute
wave: 7
depends_on: ["07-06"]
files_modified:
  - pipeline/tests/test_country_blind.py
  - pipeline/tests/verify_golden.py
  - pipeline/PIPELINE.md
  - .planning/STATE.md
autonomous: true
requirements: [SC-1, SC-2a, SC-2b, SC-4, SC-4b]
must_haves:
  truths:
    - "A probe that imports only from common/ can round-trip progress, select a batch, and render frontmatter for a non-Israel shape"
    - "The structural criterion is asserted by a command, not by assertion in prose"
    - "PIPELINE.md documents the common/ layout and the single supported invocation form"
    - "The phase closes with laws/israel/ byte-identical to HEAD and the full gate green"
  artifacts:
    - path: "pipeline/tests/test_country_blind.py"
      provides: "SC-4 proof: non-Israel caller using only common/"
      contains: "def main"
  key_links:
    - from: "pipeline/tests/test_country_blind.py"
      to: "pipeline/common/"
      via: "imports only common.* plus stdlib"
      pattern: "from common\\."
    - from: "pipeline/tests/verify_golden.py"
      to: "pipeline/tests/test_country_blind.py"
      via: "--structure and --country-blind wired into the default suite"
      pattern: "country.blind"
---

<objective>
Close the phase: prove country-blindness rather than assert it, turn on the structural gate, document
the new layout, and leave the repository in a state a reviewer can verify with one command.

Purpose: success criterion 4 says "a new country package can read/write frontmatter and progress
without copying a single line of Israel-specific code". That is only credible if a program actually
does it. This plan writes that program.
Output: `pipeline/tests/test_country_blind.py`; `--structure` and `--country-blind` wired into the
default gate; updated `pipeline/PIPELINE.md` and `.planning/STATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shared-pipeline-core/07-RESEARCH.md
@.planning/phases/07-shared-pipeline-core/07-PATTERNS.md
@.planning/phases/07-shared-pipeline-core/07-VALIDATION.md
@.planning/phases/07-shared-pipeline-core/07-06-SUMMARY.md
@pipeline/PIPELINE.md
@CLAUDE.md

<interfaces>
The complete country-blind surface after plans 07-03 through 07-06:

pipeline/common/frontmatter.py
    split_frontmatter(text: str) -> tuple[str, str]
    render_frontmatter(lines: list[str]) -> str
    quote(value: str) -> str

pipeline/common/progress.py
    load_progress(path: Path) -> dict
    save_progress(path: Path, progress: dict) -> None
    get_next_batch(manifest, progress, count, id_keys=("law_id","bill_id"), source_key="pdf_path") -> list[dict]
    print_status(manifest, progress, source_label="With PDF") -> None

pipeline/common/deploy.py
    deploy(site_dir: Path, env_overrides: dict | None = None) -> bool   # NEVER call in this phase

Symbols that must no longer be DEFINED in the Israel modules (SC-1 structural check):
    link_resolver.py    split_frontmatter
    cross_linker.py     _split_frontmatter
    batch_import.py     load_progress, save_progress, get_next_batch, print_status, deploy
                        (each now exists only as a 1-2 line delegating wrapper)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the country-blindness probe and turn on the structural gate</name>
  <files>pipeline/tests/test_country_blind.py, pipeline/tests/verify_golden.py</files>
  <read_first>
    - .planning/phases/07-shared-pipeline-core/07-PATTERNS.md, section "pipeline/tests/test_country_blind.py" — the import-boundary requirement, the synthetic non-Israel shapes to exercise, and the static boundary assertions
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, "Recommended Extraction Sequence", the paragraph defining how criterion 4 is proven rather than asserted
    - pipeline/tests/verify_golden.py (the `--structure` check written in 07-02, currently opt-in and red)
    - pipeline/common/frontmatter.py, pipeline/common/progress.py, pipeline/common/deploy.py
  </read_first>
  <action>
    Create `pipeline/tests/test_country_blind.py`. Same bootstrap shape as the other harness scripts
    (`PIPELINE_DIR = Path(__file__).parent.parent`, `sys.path.insert(0, str(PIPELINE_DIR))`,
    `# noqa: E402` on post-bootstrap imports), but with one hard rule that is the entire point of the
    file: it may import ONLY from `common.*` plus stdlib. It must never import `reconcile`,
    `batch_import`, `link_resolver`, or `cross_linker` — so copy `backfill_seo_meta.py`'s bootstrap
    shape but NOT its `from reconcile import ...` line.

    Exercise the full country-blind surface with deliberately non-Israel shapes, in a
    `tempfile.mkdtemp()` directory:
      - `save_progress(tmp/"p.json", {...})` then `load_progress` it back and assert equality; also
        assert `load_progress` on a non-existent path returns the empty-state default
      - `get_next_batch` over a synthetic manifest whose rows use `doc_id` (not `law_id`/`bill_id`)
        and `xml_path` (not `pdf_path`), passing `id_keys=("doc_id",)` and `source_key="xml_path"`.
        Create the referenced files in the temp dir so the existence check is meaningful, and assert
        both the priority-queue-drains-first behaviour and that done/failed ids are skipped
      - `render_frontmatter` with UK-shaped fields (for example a title, a nation, and an SPDX-style
        licence line) and assert the result opens and closes with a fence line and ends with a newline
      - `quote` on a value containing an embedded double quote

    `main()` returns `int` and the CLI block is `raise SystemExit(main())`, matching the repo's
    exit-code contract. Print one line per assertion group so a reviewer can read the proof.

    Then wire two things into `pipeline/tests/verify_golden.py`:
      - add a `--country-blind` flag that runs `test_country_blind.py` as a subprocess and fails on a
        non-zero exit code
      - add `--structure` AND `--country-blind` to the default (no-flag) full suite. Both were opt-in
        until now because `--structure` could not pass before 07-06 landed. Do not weaken either
        check to make it pass — if `--structure` is red, the fix is in the production module, not in
        the check.

    Do NOT call `deploy()` from the probe. Assert only that `common.deploy.deploy` is importable and
    that `inspect.signature` shows `site_dir` as a required parameter — executing it would publish to
    production GitHub Pages.
  </action>
  <verify>
    <automated>~/.venv-codex/bin/python pipeline/tests/test_country_blind.py && ! grep -nE "^\s*(from|import) (reconcile|batch_import|link_resolver|cross_linker)" pipeline/tests/test_country_blind.py && ~/.venv-codex/bin/python pipeline/tests/verify_golden.py --structure && ~/.venv-codex/bin/python pipeline/tests/verify_golden.py --country-blind && echo COUNTRY_BLIND_OK</automated>
  </verify>
  <acceptance_criteria>
    - `~/.venv-codex/bin/python pipeline/tests/test_country_blind.py` exits 0
    - Source assertion: `grep -nE "^\s*(from|import) (reconcile|batch_import|link_resolver|cross_linker)" pipeline/tests/test_country_blind.py` returns no matches
    - Source assertion: every non-stdlib import in the probe starts with `from common.` or `import common`
    - Behaviour assertion: the probe round-trips progress through a temp path and gets back an equal dict
    - Behaviour assertion: the probe selects a batch using `id_keys=("doc_id",)` and `source_key="xml_path"` with no Israel key names anywhere in the synthetic manifest
    - Behaviour assertion: the probe renders a frontmatter block from UK-shaped fields that opens and closes with a fence and ends with a newline
    - Source assertion: `grep -c 'deploy(' pipeline/tests/test_country_blind.py` shows no invocation of `deploy(` with arguments — only `inspect.signature` inspection
    - `~/.venv-codex/bin/python pipeline/tests/verify_golden.py --structure` exits 0 (SC-1 now provable)
    - `~/.venv-codex/bin/python pipeline/tests/verify_golden.py --country-blind` exits 0
    - Source assertion: the default (no-flag) suite in `verify_golden.py` now includes both the structure and country-blind checks
  </acceptance_criteria>
  <done>Criterion 4 is proven by an executing program that touches no Israel code, and the structural criterion is enforced by the default gate.</done>
</task>

<task type="auto">
  <name>Task 2: Document the layout, record the deferred defects, and run the phase-exit gate</name>
  <files>pipeline/PIPELINE.md, .planning/STATE.md</files>
  <read_first>
    - pipeline/PIPELINE.md (current pipeline documentation to update)
    - .planning/STATE.md, sections "Decisions", "Known Constraints", and "Todos"
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, "Recommended Extraction Sequence" (Wave 2 close-out), "Pitfall 1" (the redefined gate), "Pitfall 4" (supported invocation form), "Pitfall 7" (stale requirements.txt)
    - .planning/ROADMAP.md, the Phase 7 section (four success criteria)
  </read_first>
  <action>
    Update `pipeline/PIPELINE.md`:
      - document the `pipeline/common/` layout: `frontmatter.py` (split / render / quote),
        `progress.py` (load / save / next-batch / status), `deploy.py` (site deploy), and the rule
        that `common/` never imports a country module
      - document `python pipeline/X.py` as the ONLY supported invocation form, and state explicitly
        that `python -m pipeline.X` is not supported and never was (`run_batch` does a bare
        `import link_resolver` with no `sys.path` bootstrap, which raises under `-m`). Do not "fix"
        this by adding `pipeline/__init__.py` — that would break every documented entry point.
      - document the characterization harness: `pipeline/tests/capture_golden.py` re-baselines the
        fixtures, `pipeline/tests/verify_golden.py` is the gate, and the golden fixtures are
        invalidated by any commit touching `laws/israel/`, `link_resolver.py`, `reconcile.py`, or
        `data/raw/israel/import_progress.json`
      - note the mandatory interpreter `~/.venv-codex/bin/python`

    Update `.planning/STATE.md`:
      - add a Decisions row dated 2026-08-09 recording the redefined Phase 7 exit gate: the literal
        ROADMAP wording ("re-run pipeline, `git diff --stat laws/israel/` must be empty") is
        unachievable because of a pre-existing, unrelated `_STRIP_MG_INDEX` idempotency bug, so the
        gate is a BEFORE/AFTER diff-fingerprint differential plus a `git status --porcelain
        laws/israel/` empty assertion at every checkpoint — strictly stronger than the original
        wording, since the committed Israel content is never touched by this phase
      - confirm the two existing Todos remain open and are NOT closed by this phase: the
        `_STRIP_MG_INDEX` sidenote-corruption bug (deferred by user request to a v1.0 Phase 4
        follow-up) and the stale `pipeline/requirements.txt` (missing `python-dotenv` and
        `google-genai`)
      - record the off-repo backup location and restore command from 07-01

    Do NOT touch `link_resolver._STRIP_MG_INDEX` or its matching behaviour. The user explicitly
    deferred that fix; changing it would alter `laws/israel/` content and invalidate every fixture.

    Phase-exit gate, run last:
      1. `~/.venv-codex/bin/python pipeline/tests/verify_golden.py` — full suite, now including
         `--structure` and `--country-blind`, must exit 0
      2. `~/.venv-codex/bin/python pipeline/batch_import.py --status` diffs clean against
         `pipeline/tests/golden/status.txt`
      3. `test -z "$(git status --porcelain laws/israel/)"`
      4. `git diff --stat laws/israel/` is empty — criterion 2 as literally written, satisfied
         because the committed Israel content was never modified by this phase

    Stage explicitly: `git add pipeline/PIPELINE.md pipeline/tests .planning/STATE.md`. Never
    `git add -A`.
  </action>
  <verify>
    <automated>grep -q 'common/' pipeline/PIPELINE.md && grep -q 'verify_golden' pipeline/PIPELINE.md && grep -q 'STRIP_MG_INDEX' .planning/STATE.md && ~/.venv-codex/bin/python pipeline/tests/verify_golden.py && ~/.venv-codex/bin/python pipeline/batch_import.py --status | diff pipeline/tests/golden/status.txt - && test -z "$(git status --porcelain laws/israel/)" && test -z "$(git diff --stat laws/israel/)" && echo PHASE_EXIT_GATE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/PIPELINE.md` documents `pipeline/common/{frontmatter,progress,deploy}.py`, the one-way dependency rule, `python pipeline/X.py` as the only supported invocation form, the harness commands, and `~/.venv-codex/bin/python`
    - `.planning/STATE.md` contains a Decisions row recording the redefined exit gate and the reason (pre-existing `_STRIP_MG_INDEX` bug)
    - `.planning/STATE.md` still lists both deferred defects as open Todos (`_STRIP_MG_INDEX`, stale `requirements.txt`)
    - Source assertion: `git diff HEAD -- pipeline/link_resolver.py | grep -c '_STRIP_MG_INDEX'` returns 0 across the whole phase (the deferred bug was not touched)
    - `~/.venv-codex/bin/python pipeline/tests/verify_golden.py` (full suite incl. structure and country-blind) exits 0
    - `~/.venv-codex/bin/python pipeline/batch_import.py --status` diffs clean against `golden/status.txt`
    - `test -z "$(git status --porcelain laws/israel/)"` passes
    - `test -z "$(git diff --stat laws/israel/)"` passes — criterion 2 as literally written
    - `git diff --cached --name-only` lists only `pipeline/PIPELINE.md`, paths under `pipeline/tests/`, and `.planning/STATE.md`
  </acceptance_criteria>
  <done>The phase is documented, the deferred defects are recorded and untouched, and one command proves all four success criteria with `laws/israel/` byte-identical to HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temp filesystem to `common.progress` | The probe writes synthetic progress/manifest data into a `tempfile.mkdtemp()` sandbox, never the real `data/raw/` |
| pipeline to git working tree (`laws/israel/`) | The gate's link-resolver run mutates tracked content before restoring it |
| `common/` to Israel modules | One-way dependency edge, now asserted by both a static grep and an executing probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | `test_country_blind.py` | mitigate | Probe imports only `common.*` plus stdlib, so it never triggers `dotenv` loading; no `GEMINI_API_KEY` reference anywhere under `pipeline/tests/` (ASVS V14) |
| T-07-02 | Tampering (integrity) | `laws/israel/*.md`; auto-deploy on push to `main` | mitigate | Phase-exit gate asserts both `git status --porcelain laws/israel/` and `git diff --stat laws/israel/` are empty before the final commit; explicit path staging (ASVS V12) |
| T-07-04 | Denial of service | `data/raw/israel/import_progress.json` | mitigate | The probe operates entirely inside `tempfile.mkdtemp()`; it never touches the real progress path (ASVS V12) |
| T-07-10 | Tampering (integrity) | Accidental production publication | mitigate | The probe only inspects `deploy`'s signature; it never invokes it. Recorded as the phase's manual-only verification |
| T-07-14 | Repudiation | Structural criterion asserted in prose rather than checked | mitigate | `--structure` and `--country-blind` are wired into the default suite, so SC-1 and SC-4 fail loudly on regression rather than depending on a reviewer's memory |
| T-07-SC | Tampering | pip installs | accept | Zero new runtime dependencies across the entire phase; `pytest` deliberately never installed |
</threat_model>

<verification>
- `test_country_blind.py` exits 0 and imports no Israel module
- `verify_golden.py --structure` and `--country-blind` both exit 0
- Full `verify_golden.py` (now including both) exits 0
- `--status` diffs clean against the golden fixture
- `git status --porcelain laws/israel/` and `git diff --stat laws/israel/` both empty
- `_STRIP_MG_INDEX` untouched across the whole phase
</verification>

<success_criteria>
All four ROADMAP success criteria for Phase 7 are provable by command:
SC-1 via `verify_golden.py --structure`; SC-2 via the diff-fingerprint differential plus the empty
`git diff --stat laws/israel/`; SC-3 via the byte-diff of `--status` against `golden/status.txt`;
SC-4 via `test_country_blind.py`.
</success_criteria>

<output>
Create `.planning/phases/07-shared-pipeline-core/07-07-SUMMARY.md` when done. Record the four
criterion-to-command mappings with their observed output, and restate the two defects left open.
</output>
