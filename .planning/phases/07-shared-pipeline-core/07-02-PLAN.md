---
phase: 07-shared-pipeline-core
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - pipeline/tests/verify_golden.py
autonomous: true
requirements: [SC-2a, SC-2b, SC-2c, SC-2d, SC-3, SC-3b, SC-3c]
must_haves:
  truths:
    - "A single command re-runs every BEFORE fingerprint and reports pass/fail with a non-zero exit code on any mismatch"
    - "The gate demonstrably fails when behaviour changes (proven with a negative control, then restored)"
    - "Running the full gate leaves laws/israel/ byte-identical to HEAD"
  artifacts:
    - path: "pipeline/tests/verify_golden.py"
      provides: "The Phase 7 gate: --quick, --split, --frontmatter, --batch, --progress-roundtrip, --status, --structure, and full suite"
      contains: "def main"
  key_links:
    - from: "pipeline/tests/verify_golden.py"
      to: "pipeline/tests/golden/"
      via: "read each fixture and diff against a fresh run"
      pattern: "golden"
    - from: "pipeline/tests/verify_golden.py"
      to: "shell exit code"
      via: "raise SystemExit(main(...))"
      pattern: "SystemExit"
---

<objective>
Build the gate every subsequent extraction step must pass: one stdlib script that re-derives all
four fingerprints and diffs them against the committed fixtures, returning 0 only when everything
is byte-identical.

Purpose: this is the mechanism that turns "zero behaviour change" from an intention into a
mechanically checkable fact. CLAUDE.md: "Validation errors must never be silently ignored."
Output: `pipeline/tests/verify_golden.py`, proven green against unmodified source AND proven to go
red under a deliberate perturbation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-shared-pipeline-core/07-RESEARCH.md
@.planning/phases/07-shared-pipeline-core/07-PATTERNS.md
@.planning/phases/07-shared-pipeline-core/07-VALIDATION.md
@.planning/phases/07-shared-pipeline-core/07-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Fixtures produced by plan 07-01 that this plan consumes:

pipeline/tests/golden/frontmatter_111.json   json.dumps(mapping, ensure_ascii=False, sort_keys=True); sha256 == dab1887e06dd074051ed6a2eff2c2fcdabff1a2f523c8a3cc93fd20c7168dd37
pipeline/tests/golden/next_batch.json        {"1": [...], "5": [...], "25": [...], "100": [...]}; d["5"][:3] == ["2001111","2001108","2001070"]
pipeline/tests/golden/status.txt             exact 6-line stdout of `batch_import.py --status`
pipeline/tests/golden/link_resolver_all.diff `git diff laws/israel/` after one `link_resolver.py --all` from a clean HEAD

Harness helpers already written in pipeline/tests/capture_golden.py (import or mirror them, do not
re-derive): the frozen-clock shim, the sha256-over-canonical-json fingerprint, and the
checkout / purge-pycache / run / diff / checkout sequence.

Current production symbols under test (line numbers verified 2026-08-09):
  link_resolver.split_frontmatter(text) -> tuple[str, str]      line 27
  cross_linker._split_frontmatter(text) -> tuple[str, str]      line 202
  reconcile.build_frontmatter(entry) -> str                     line 120
  batch_import.load_progress() / save_progress(progress)        lines 47, 54
  batch_import.get_next_batch(manifest, progress, count)        line 255
  batch_import.print_status(manifest, progress)                 line 296
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pipeline/tests/verify_golden.py with the full flag set</name>
  <files>pipeline/tests/verify_golden.py</files>
  <read_first>
    - .planning/phases/07-shared-pipeline-core/07-PATTERNS.md, section "pipeline/tests/verify_golden.py" (argparse + exit-code contract, logging pattern, "never silently pass a diff") and section "Shared Patterns" (error handling, UTF-8 I/O, interpreter)
    - .planning/phases/07-shared-pipeline-core/07-VALIDATION.md, section "Per-Task Verification Map" — the flag set is dictated by this table
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, section "Code Examples" — the splitter-equivalence script, the frontmatter verify mode, the batch fingerprint, the progress round-trip check, and the shell form of the link-resolver differential
    - pipeline/tests/capture_golden.py (reuse its shim and fingerprint helpers)
    - pipeline/link_resolver.py lines 235-265 (main() -> int and the argparse/SystemExit CLI contract to mirror)
  </read_first>
  <action>
    Create `pipeline/tests/verify_golden.py`. Stdlib only. Same bootstrap as `capture_golden.py`:
    `PIPELINE_DIR = Path(__file__).parent.parent`, `PROJECT_DIR = PIPELINE_DIR.parent`,
    `sys.path.insert(0, str(PIPELINE_DIR))`, post-bootstrap imports with `# noqa: E402`.

    Implement one check function per flag. Each returns True on pass, and on failure logs the
    concrete mismatch (expected vs actual, or the first differing line) via `logging.error` with
    `%s`-style lazy args and returns False. A non-empty diff is a failure, never a warning.

      --split                Differential over the real corpus. For every file in
                             `sorted(Path("laws/israel").glob("*.md"))` (112 files incl. index.md),
                             assert `link_resolver.split_frontmatter(t) == cross_linker._split_frontmatter(t)`.
                             Then assert the same for the six edge cases from research:
                             "", "no frontmatter", "---\nx: 1\n---\nbody", "---\nx: 1\n---\n\nbody",
                             "---\nonly-open\n", "---\n---\n". Expect 112 files, 0 mismatches.
                             This check stays meaningful after the extraction because both names
                             still resolve (one direct import, one alias).
      --frontmatter          Install the frozen-clock shim, rebuild the 111-entry mapping exactly as
                             capture_golden does, serialise identically
                             (`json.dumps(mapping, ensure_ascii=False, sort_keys=True)`), and compare
                             the string to `golden/frontmatter_111.json`. Also assert its sha256 is
                             dab1887e06dd074051ed6a2eff2c2fcdabff1a2f523c8a3cc93fd20c7168dd37. On
                             mismatch, report the first differing law_id, not just the hash.
      --batch                Recompute the selection at counts 1/5/25/100 and compare to
                             `golden/next_batch.json`.
      --progress-roundtrip   Read `data/raw/israel/import_progress.json` as bytes, `json.loads` it,
                             re-serialise into an in-memory `io.StringIO` with
                             `json.dump(obj, buf, ensure_ascii=False, indent=2)`, and assert the
                             encoded result equals the original bytes. Byte-fidelity details that
                             must hold: `ensure_ascii=False`, `indent=2`, and NO trailing newline
                             (the live 1867-byte file ends with '}'). This check must operate
                             entirely in memory — it must NEVER write to the real PROGRESS_PATH.
      --status               Run `[sys.executable, str(PIPELINE_DIR / "batch_import.py"), "--status"]`
                             with cwd=PROJECT_DIR and diff stdout against `golden/status.txt`.
                             `--status` is the only permitted invocation form of batch_import.py.
      --structure            SC-1 structural check. Assert `pipeline/common/__init__.py`,
                             `common/frontmatter.py`, `common/progress.py`, `common/deploy.py` exist;
                             assert `split_frontmatter`, `_split_frontmatter`, `load_progress`,
                             `save_progress`, `get_next_batch`, `print_status`, `deploy` are no
                             longer *defined* (regex on `^def NAME`) in the Israel modules that gave
                             them up; and assert the boundary — no file under `pipeline/common/`
                             matches `^\s*(from|import) (reconcile|batch_import|link_resolver|cross_linker)`.
                             This flag is OPT-IN and is expected to FAIL until plan 07-06 lands; it
                             is deliberately excluded from the default suite (see below).
      --quick                Composite: --split + --frontmatter + --status + --progress-roundtrip +
                             --batch. Mutates no file. Target runtime around 2s. Use
                             `logging.basicConfig(level=logging.WARNING, ...)` for this branch,
                             mirroring `batch_import.py:435`'s read-only branch.
      (no flag)              Full suite: everything in --quick PLUS the link-resolver differential:
                             `git checkout -- laws/israel/`; purge `__pycache__`; run
                             `link_resolver.py --all`; write `git diff laws/israel/` to a temp file;
                             `git checkout -- laws/israel/`; diff that temp file against
                             `golden/link_resolver_all.diff` (must be empty); then assert
                             `git status --porcelain laws/israel/` is empty. Fail loudly if the
                             restore did not happen — an unrestored tree is a publish risk.
                             --structure is NOT part of the default suite.

    `main(...) -> int` returns 0 only when every selected check passed, 1 otherwise, and prints a
    one-line-per-check PASS/FAIL summary. CLI block mirrors `link_resolver.py:258-265`:
    `parser = argparse.ArgumentParser(description=__doc__)`, `store_true` flags,
    `raise SystemExit(main(...))`. Configure logging inside `main()`, never at import.

    All subprocess calls are list-form with no `shell=True`. The script never calls `git add`, never
    calls `save_progress` against the real path, and never invokes `deploy()` or
    `batch_import.py` without `--status`.
  </action>
  <verify>
    <automated>~/.venv-codex/bin/python -c "import ast; ast.parse(open('pipeline/tests/verify_golden.py').read()); print('PARSE_OK')" && for f in --split --frontmatter --batch --progress-roundtrip --status --structure --quick; do grep -q -- "$f" pipeline/tests/verify_golden.py || { echo "MISSING $f"; exit 1; }; done && test "$(grep -v '^#' pipeline/tests/verify_golden.py | grep -c 'shell=True')" = 0 && echo FLAGS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/tests/verify_golden.py` parses under `~/.venv-codex/bin/python`
    - All seven flags are present in the source: `--split`, `--frontmatter`, `--batch`, `--progress-roundtrip`, `--status`, `--structure`, `--quick`
    - Source assertion: ends with `raise SystemExit(main(` and `main` is annotated `-> int`
    - Source assertion: `grep -v '^#' pipeline/tests/verify_golden.py | grep -c 'shell=True'` returns 0
    - Source assertion: `grep -v '^#' pipeline/tests/verify_golden.py | grep -c 'save_progress'` returns 0 (round-trip check is in-memory only)
    - Source assertion: every line matching `batch_import.py` also contains `--status`
    - Source assertion: contains the literal `dab1887e06dd074051ed6a2eff2c2fcdabff1a2f523c8a3cc93fd20c7168dd37`
    - Source assertion: the default (no-flag) path contains both a `git checkout` restore and a `git status --porcelain laws/israel/` assertion
  </acceptance_criteria>
  <done>A single stdlib gate script exists covering every criterion in 07-VALIDATION.md's verification map, with an int exit-code contract and no file-mutating side effects outside the restore-protected link-resolver run.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the gate is green now and red under perturbation</name>
  <files>pipeline/tests/verify_golden.py</files>
  <read_first>
    - pipeline/tests/verify_golden.py (written in Task 1)
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, "Pitfall 2" (restore discipline) and "Pitfall 5" (interpreter)
    - .planning/phases/07-shared-pipeline-core/07-VALIDATION.md, section "Sampling Rate"
  </read_first>
  <action>
    Positive control. From the repo root run `~/.venv-codex/bin/python pipeline/tests/verify_golden.py --quick`
    then the full suite `~/.venv-codex/bin/python pipeline/tests/verify_golden.py`. Both must exit 0
    against completely unmodified production source. If any check fails here, the fixture is wrong,
    not the code — stop and re-baseline via 07-01 rather than loosening the check.

    Negative control — this is the point of the task. A gate that has never been observed failing is
    not known to have teeth. Perturb ONE production value in a trivially reversible way, confirm the
    gate goes red, then restore and confirm it goes green again. Use `pipeline/reconcile.py` line 179
    (`generated_by: pipeline/reconcile.py`) as the perturbation target: change that literal string,
    run `verify_golden.py --frontmatter`, assert the exit code is 1 and the log names a differing
    law_id, then `git checkout -- pipeline/reconcile.py` and re-run to assert exit 0. Do the same
    one-line perturbation experiment for `--status` by temporarily changing a label in
    `batch_import.py:302`, then restoring.

    Restore with `git checkout -- pipeline/reconcile.py pipeline/batch_import.py` and verify
    `git status --porcelain pipeline/` shows only the new `verify_golden.py`. Never `git add -A`.

    Record both control results verbatim in the SUMMARY — the exit codes and the failure messages —
    so a later reviewer can see the gate discriminates.

    Then assert `git status --porcelain laws/israel/` is empty and commit with
    `git add pipeline/tests/verify_golden.py`.
  </action>
  <verify>
    <automated>~/.venv-codex/bin/python pipeline/tests/verify_golden.py --quick && ~/.venv-codex/bin/python pipeline/tests/verify_golden.py && test -z "$(git status --porcelain laws/israel/)" && test -z "$(git status --porcelain pipeline/reconcile.py pipeline/batch_import.py pipeline/link_resolver.py pipeline/cross_linker.py)" && echo GATE_GREEN_SOURCE_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - `verify_golden.py --quick` exits 0 against unmodified source
    - `verify_golden.py` (full suite) exits 0 against unmodified source
    - Negative control recorded: with `reconcile.py` line 179 perturbed, `verify_golden.py --frontmatter` exits 1 and the log names at least one differing law_id
    - Negative control recorded: with `batch_import.py` line 302 perturbed, `verify_golden.py --status` exits 1
    - After restore, `git status --porcelain pipeline/reconcile.py pipeline/batch_import.py` is empty
    - `test -z "$(git status --porcelain laws/israel/)"` passes after the full-suite run
    - `git diff --cached --name-only` lists only `pipeline/tests/verify_golden.py`
  </acceptance_criteria>
  <done>The Phase 7 gate is committed, observed green on unmodified source, and observed red on a deliberate one-line behaviour change — with all production source restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem to pipeline scripts | Local JSON/Markdown crosses here via `json.load` and `read_text` |
| pipeline to git working tree (`laws/israel/`) | The full suite mutates tracked content in place before restoring it |
| pipeline to external process | List-form `subprocess.run` calls to git and python |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | `verify_golden.py` importing `reconcile` (which calls `load_dotenv` on `pipeline/.env`) | mitigate | Gate never reads, prints, or persists `GEMINI_API_KEY`; assert `grep -c 'GEMINI_API_KEY' pipeline/tests/verify_golden.py` returns 0 (ASVS V14) |
| T-07-02 | Tampering (integrity) | `laws/israel/*.md`; auto-deploy on push to `main` | mitigate | Full suite always restores with `git checkout -- laws/israel/` and asserts `git status --porcelain laws/israel/` empty; a failed restore is a hard failure, not a warning (ASVS V12) |
| T-07-03 | Tampering | `subprocess.run` to git / python / batch_import | mitigate | List-form argv only, no `shell=True`, no caller-supplied string interpolated into argv; `batch_import.py` only ever invoked with `--status` (ASVS V7) |
| T-07-04 | Denial of service | `data/raw/israel/import_progress.json` | mitigate | `--progress-roundtrip` operates entirely in memory via `io.StringIO`; the gate never calls `save_progress` against the real path. Off-repo backup from 07-01 is the fallback |
| T-07-05 | Repudiation | Silent gate degradation (a check that always passes) | mitigate | Task 2's negative control proves the gate discriminates; both control results recorded in the SUMMARY |
| T-07-SC | Tampering | pip installs | accept | Zero new runtime dependencies; `pytest` deliberately not installed (research recommends stdlib scripts) |
</threat_model>

<verification>
- `verify_golden.py --quick` exits 0; full suite exits 0
- Negative controls produce exit code 1 with an informative message, then restore cleanly
- `git status --porcelain laws/israel/` empty after every run
- Only `pipeline/tests/verify_golden.py` is added to the index
</verification>

<success_criteria>
Every later plan in this phase can prove zero behaviour change with one command, and the gate has
been observed both passing and failing, so a green result is meaningful.
</success_criteria>

<output>
Create `.planning/phases/07-shared-pipeline-core/07-02-SUMMARY.md` when done. Record the exact
command set, the positive-control output, and both negative-control failure messages verbatim.
</output>
