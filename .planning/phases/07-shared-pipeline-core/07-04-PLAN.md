---
phase: 07-shared-pipeline-core
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - pipeline/common/progress.py
  - pipeline/batch_import.py
autonomous: true
requirements: [SC-1, SC-3, SC-3b, SC-3c, SC-4]
must_haves:
  truths:
    - "Progress load/save, batch selection, and status printing live in a country-blind module parameterised by path, id keys, source key, and label"
    - "python pipeline/batch_import.py --status still prints the exact same 6 lines"
    - "get_next_batch selects the same laws in the same order at counts 1/5/25/100"
    - "save_progress still writes byte-identical JSON with no trailing newline"
  artifacts:
    - path: "pipeline/common/progress.py"
      provides: "load_progress, save_progress, get_next_batch, print_status"
      contains: "def get_next_batch"
  key_links:
    - from: "pipeline/batch_import.py"
      to: "common.progress"
      via: "thin module-level wrappers bound to PROGRESS_PATH; all 12 call sites unchanged"
      pattern: "from common import progress as _progress"
---

<objective>
Second extraction: move the progress-file handling and batch-selection algorithm into
`pipeline/common/progress.py`, parameterised so a non-Israel caller can use it with different id
keys, a different source-artifact key, and a different status label.

Purpose: `{done, failed, total_deployed, priority}` bookkeeping and the drain-priority-then-manifest
scheduling idea contain zero country content — only `PROGRESS_PATH`, the `law_id`/`bill_id` key
names, the `pdf_path` key, and the "With PDF" label differ. This is the extraction that makes
success criterion 4 real.
Output: `pipeline/common/progress.py`; `batch_import.py` reduced to four thin wrappers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-shared-pipeline-core/07-RESEARCH.md
@.planning/phases/07-shared-pipeline-core/07-PATTERNS.md
@.planning/phases/07-shared-pipeline-core/07-VALIDATION.md
@.planning/phases/07-shared-pipeline-core/07-03-SUMMARY.md
@CLAUDE.md

<interfaces>
Current definitions being moved (line numbers verified in the working tree 2026-08-09):
  pipeline/batch_import.py
    load_progress() -> dict                                     line 47
    save_progress(progress: dict) -> None                       line 54
    get_next_batch(manifest, progress, count) -> list[dict]     line 255
    print_status(manifest, progress) -> None                    line 296
    PROGRESS_PATH = DATA_DIR / "import_progress.json"           line 37

Target signatures in pipeline/common/progress.py:
    load_progress(path: Path) -> dict
    save_progress(path: Path, progress: dict) -> None
    get_next_batch(manifest: list[dict], progress: dict, count: int,
                   id_keys: tuple[str, ...] = ("law_id", "bill_id"),
                   source_key: str = "pdf_path") -> list[dict]
    print_status(manifest: list[dict], progress: dict, source_label: str = "With PDF") -> None

The 12 existing call sites in batch_import.py that MUST NOT change:
  line 332 progress = load_progress()
  line 333 batch = get_next_batch(manifest, progress, count)
  lines 337, 360, 368, 423 print_status(manifest, progress)
  lines 357, 409, 419 save_progress(progress)
  line 437 progress = load_progress()
  line 438 print_status(manifest, progress)

Byte-fidelity facts, verified by execution:
  save_progress writes json.dump(progress, f, ensure_ascii=False, indent=2) and NO trailing newline;
  the live file is 1867 bytes and ends with '}'.
  --status stdout is exactly 6 lines with values 1076 / 718 / 111 / 0 / 607 / 111.
  get_next_batch at counts >= 5 starts with ['2001111', '2001108', '2001070'].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline/common/progress.py</name>
  <files>pipeline/common/progress.py</files>
  <read_first>
    - .planning/phases/07-shared-pipeline-core/07-PATTERNS.md, section "pipeline/common/progress.py" — VERBATIM excerpts of all four functions, the dashed section-banner style, the identity-check warning, and the column-alignment warning
    - pipeline/batch_import.py lines 18-70 (header, constants, load/save) and 251-313 (get_next_batch, print_status)
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, section "Verified Extraction Targets" and "Architectural Responsibility Map"
  </read_first>
  <action>
    Create `pipeline/common/progress.py`. Stdlib only: `json`, `pathlib`. Header per 07-PATTERNS.md,
    using the DASHED section-banner style (`# ---...` with a `Progress tracking` caption) because
    that is the style of `batch_import.py`, the file this code comes from.

    Port all four functions VERBATIM from the excerpts in 07-PATTERNS.md, changing only these names:

      load_progress(path)             — replace `PROGRESS_PATH` with `path`. Keep `path.exists()`,
                                        `open(path, encoding="utf-8")`, `json.load(f)`, and the exact
                                        default `{"done": [], "failed": [], "total_deployed": 0, "priority": []}`.
      save_progress(path, progress)   — replace `PROGRESS_PATH` with `path`. Keep
                                        `open(path, "w", encoding="utf-8")` and
                                        `json.dump(progress, f, ensure_ascii=False, indent=2)`
                                        EXACTLY. Do not "improve" this to
                                        `path.write_text(json.dumps(...) + "\n")` — `json.dump`
                                        writes no trailing newline and the live file has none.
      get_next_batch(...)             — parameterise exactly three literal-name sites: the `by_id`
                                        comprehension, the `law_id = entry.get(...)` line, and the
                                        two `entry.get("pdf_path")` / `Path(entry["pdf_path"]).exists()`
                                        checks. Everything else is verbatim.
                                        PRESERVE EXACTLY the line `if any(e is entry for e in batch):`
                                        (currently batch_import.py line 287). That is IDENTITY
                                        comparison, deliberately: manifest rows are dicts, unhashable
                                        and possibly equal-but-distinct. Do NOT modernise it to
                                        `if entry in batch` or to a set of ids — that changes
                                        selection and breaks the next_batch fingerprint.
      print_status(...)               — add `source_label: str = "With PDF"`. Column alignment is
                                        load-bearing: the gate is a byte-diff of the 6 lines against
                                        `golden/status.txt`, where the label is padded to column 18.
                                        If substituting the label into the f-string risks the
                                        alignment, hard-code the default padding and substitute only
                                        the label text.

    ASVS V5: `json.load` is already the correct choice — preserve it. Never `eval`, never `pickle`.
    ASVS V12: paths are caller-supplied `Path` objects only; introduce no user-controlled path join.

    Boundary constraint: imports stdlib only. Never `reconcile`, `batch_import`, `link_resolver`,
    `cross_linker`; never reads `pipeline/.env`.

    Do not modify `batch_import.py` in this task — that is Task 2.
  </action>
  <verify>
    <automated>~/.venv-codex/bin/python -c "
import sys, json, tempfile, pathlib
sys.path.insert(0,'pipeline')
from common.progress import load_progress, save_progress, get_next_batch
d = pathlib.Path(tempfile.mkdtemp())/'p.json'
assert load_progress(d) == {'done': [], 'failed': [], 'total_deployed': 0, 'priority': []}
obj = json.loads(pathlib.Path('data/raw/israel/import_progress.json').read_text(encoding='utf-8'))
save_progress(d, obj)
assert d.read_bytes() == pathlib.Path('data/raw/israel/import_progress.json').read_bytes(), 'byte mismatch'
print('PROGRESS_ROUNDTRIP_OK')
" && test "$(grep -c 'if any(e is entry for e in batch):' pipeline/common/progress.py)" = 1 && test "$(grep -rvE '^\s*#' pipeline/common/progress.py | grep -cE '(from|import) (reconcile|batch_import|link_resolver|cross_linker)')" = 0 && echo MODULE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/common/progress.py` defines `load_progress(path)`, `save_progress(path, progress)`, `get_next_batch(manifest, progress, count, id_keys=..., source_key=...)`, `print_status(manifest, progress, source_label=...)`
    - Behaviour assertion: `save_progress(tmp, json.loads(live_file))` produces bytes identical to `data/raw/israel/import_progress.json` (proves `ensure_ascii=False`, `indent=2`, no trailing newline)
    - Behaviour assertion: `load_progress(nonexistent_path)` returns `{"done": [], "failed": [], "total_deployed": 0, "priority": []}`
    - Source assertion: `grep -c 'if any(e is entry for e in batch):' pipeline/common/progress.py` returns 1
    - Source assertion: `grep -c 'ensure_ascii=False, indent=2' pipeline/common/progress.py` returns 1
    - Source assertion: `grep -c 'write_text' pipeline/common/progress.py` returns 0
    - Source assertion: default `source_label` is the literal `"With PDF"`
    - Source assertion: `grep -rvE '^\s*#' pipeline/common/progress.py | grep -cE '(from|import) (reconcile|batch_import|link_resolver|cross_linker)'` returns 0
    - Source assertion: `grep -c 'eval(\|pickle' pipeline/common/progress.py` returns 0
    - No write ever occurs against `data/raw/israel/import_progress.json` — the round-trip check writes to a temp dir
  </acceptance_criteria>
  <done>A country-blind progress module exists with byte-faithful JSON serialisation and the identity-based dedup preserved exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint batch_import.py to thin wrappers and run the gate</name>
  <files>pipeline/batch_import.py</files>
  <read_first>
    - .planning/phases/07-shared-pipeline-core/07-PATTERNS.md, section "pipeline/batch_import.py (MODIFY)" — the wrapper pattern, the 12 call sites that must not change, and the list of what MUST STAY
    - pipeline/batch_import.py lines 18-70, 225-320, 325-345, 405-440
    - .planning/phases/07-shared-pipeline-core/07-RESEARCH.md, "Pitfall 2" (restore discipline) and "Pitfall 3" (unrecoverable progress state)
  </read_first>
  <action>
    In `pipeline/batch_import.py`: delete the bodies of `load_progress` (47-52), `save_progress`
    (54-56), `get_next_batch` (255-293), and `print_status` (296-313), replacing each with a thin
    module-level wrapper that binds this module's own constants, so all 12 call sites stay untouched:

      load_progress()                        -> _progress.load_progress(PROGRESS_PATH)
      save_progress(progress)                -> _progress.save_progress(PROGRESS_PATH, progress)
      get_next_batch(manifest, progress, count) -> _progress.get_next_batch(manifest, progress, count)
      print_status(manifest, progress)       -> _progress.print_status(manifest, progress)

    Add `from common import progress as _progress` in the local-import position. The `import X as _X`
    aliasing convention already exists in this file (line 382 `import reconcile as _reconcile`,
    line 383 `from google import genai as _genai`).

    Keep the wrappers' public signatures and type hints exactly as they are today, so nothing about
    the call sites or the module's external surface changes.

    MUST STAY in `batch_import.py`, do not move any of it:
      - the `DEPLOY_EVERY` threshold block (lines 411-421) — batch-loop cadence policy, not a deploy
        mechanism; each country orchestrator owns its own
      - `load_manifest` / `save_manifest` (63-70), `_process_law`, `run_batch`
      - the deferred `import link_resolver` at line 365 with its try/except ImportError fallback at
        366-369 — deliberate, do not hoist it
      - `PROGRESS_PATH`, `SITE_DIR`, and the other module-level path constants at 31-37, which remain
        the single source of path truth
      - `deploy()` — that moves in plan 07-05, not here

    Verification, in this order:
      1. `~/.venv-codex/bin/python pipeline/batch_import.py --status` and diff against
         `pipeline/tests/golden/status.txt` — must be byte-identical, all 6 lines. `--status` is the
         ONLY permitted invocation form; running `batch_import.py` without it would resume the paused
         factory import and burn Gemini quota, violating a standing user instruction.
      2. `~/.venv-codex/bin/python pipeline/tests/verify_golden.py --batch` — selection unchanged at
         counts 1/5/25/100.
      3. `~/.venv-codex/bin/python pipeline/tests/verify_golden.py --progress-roundtrip`.
      4. `~/.venv-codex/bin/python pipeline/tests/verify_golden.py` — full suite.

    Confirm `data/raw/israel/import_progress.json` is untouched by comparing its sha256 to the value
    recorded in `$HOME/codex-civica-backups/phase07/MANIFEST.sha256` (07-01 Task 1). No task in this
    plan may write to the real progress file.

    Assert `git status --porcelain laws/israel/` is empty, then stage explicitly:
    `git add pipeline/common/progress.py pipeline/batch_import.py`. Never `git add -A`.
  </action>
  <verify>
    <automated>~/.venv-codex/bin/python pipeline/batch_import.py --status > /tmp/status_after.txt && diff pipeline/tests/golden/status.txt /tmp/status_after.txt && ~/.venv-codex/bin/python pipeline/tests/verify_golden.py && test -z "$(git status --porcelain laws/israel/)" && sha256sum data/raw/israel/import_progress.json | cut -d' ' -f1 | grep -qFf <(grep import_progress "$HOME/codex-civica-backups/phase07/MANIFEST.sha256" | cut -d' ' -f1) && echo GATE_GREEN_PROGRESS_INTACT</automated>
  </verify>
  <acceptance_criteria>
    - `diff pipeline/tests/golden/status.txt <(~/.venv-codex/bin/python pipeline/batch_import.py --status)` is empty (SC-3, exact 6-line stdout)
    - `~/.venv-codex/bin/python pipeline/tests/verify_golden.py --batch` exits 0
    - `~/.venv-codex/bin/python pipeline/tests/verify_golden.py --progress-roundtrip` exits 0
    - `~/.venv-codex/bin/python pipeline/tests/verify_golden.py` (full suite) exits 0
    - Source assertion: `grep -c 'from common import progress as _progress' pipeline/batch_import.py` returns 1
    - Source assertion: each of `load_progress`, `save_progress`, `get_next_batch`, `print_status` is defined in `batch_import.py` in a body of at most 2 lines that delegates to `_progress`
    - Source assertion: `grep -c 'DEPLOY_EVERY' pipeline/batch_import.py` is unchanged from HEAD (threshold policy stayed)
    - Source assertion: `git diff pipeline/batch_import.py` shows no change at the 12 call-site lines (332, 333, 337, 357, 360, 368, 409, 419, 423, 437, 438)
    - sha256 of `data/raw/israel/import_progress.json` matches the value recorded in the 07-01 backup manifest
    - `test -z "$(git status --porcelain laws/israel/)"` passes
    - `git diff --cached --name-only` lists only `pipeline/common/progress.py` and `pipeline/batch_import.py`
  </acceptance_criteria>
  <done>`batch_import.py` holds only path-bound wrappers over `common.progress`; `--status` output, batch selection, and the on-disk progress JSON are all provably unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem to pipeline scripts | `import_progress.json` and `manifest_laws.json` cross here via `json.load` |
| pipeline to git working tree (`laws/israel/`) | The gate's link-resolver run mutates tracked content before restoring it |
| `common/` to Israel modules | One-way dependency edge: `batch_import` imports `common.progress`, never the reverse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | `pipeline/common/progress.py` | mitigate | Stdlib-only module; never reads `pipeline/.env`, never references `GEMINI_API_KEY`, logs no env. Enforced by grep assertions (ASVS V14) |
| T-07-02 | Tampering (integrity) | `laws/israel/*.md`; auto-deploy on push to `main` | mitigate | Full gate restores with `git checkout -- laws/israel/` and asserts a clean tree before commit; explicit staging of two paths only (ASVS V12) |
| T-07-04 | Denial of service | `data/raw/israel/import_progress.json` — gitignored, unrecoverable via git | mitigate | No task writes the real progress path; the round-trip check writes to a temp dir; sha256 is compared against the 07-01 off-repo backup manifest after the edit |
| T-07-08 | Tampering | Deserialisation of the progress/manifest JSON | mitigate | `json.load` preserved verbatim; `eval`/`pickle` forbidden and grep-asserted absent (ASVS V5) |
| T-07-09 | Denial of service / quota burn | `batch_import.py` invoked without `--status` | mitigate | Only `--status` appears in any command in this plan; the standing user instruction (factory import paused at 111/718) is restated in the task action |
| T-07-SC | Tampering | pip installs | accept | Zero new runtime dependencies |
</threat_model>

<verification>
- `--status` stdout byte-identical to `golden/status.txt`
- Batch selection and progress round-trip fingerprints unchanged
- Full `verify_golden.py` exits 0
- `import_progress.json` sha256 matches the off-repo backup
- `git status --porcelain laws/israel/` empty
</verification>

<success_criteria>
Progress handling and batch selection are country-blind and parameterised, Israel's orchestrator is
reduced to path-bound wrappers, and every fingerprint from 07-01 is unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-shared-pipeline-core/07-04-SUMMARY.md` when done. Record the `--status`
diff result and the progress-file sha256 comparison.
</output>
